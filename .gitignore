# Secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# Logs: logs/trading_bot.log is a required deliverable (real testnet orders),
# so it is intentionally committed rather than ignored.
